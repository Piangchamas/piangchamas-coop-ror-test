<h1>Register Form</h1>
    <%= form_with(model: @user, local: true, url: register_path) do |f| %>
      <%= f.label :first_name %>
      <%= f.text_field :first_name, class: 'form-control' %>
    </div>
    <% end %>
